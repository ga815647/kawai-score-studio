# Kawai Score Studio

開始任何琴譜內容或軟體開發工作前，先完整讀取根目錄的 `scorebook.yaml`。

`scorebook.yaml` 是正式規格。HTML、簡譜、五線譜、播放資料與 Gate 報告都由它及其明確引用的 fixture 產生，不得直接修改生成結果來修正正式內容。

## 單一工作入口

本專案使用一個 ChatGPT Project：`Kawai Score Studio`。使用者不需要選擇「使用」或「開發」模式；助理先判斷本次是琴譜內容工作或產生器開發，再套用對應流程。

正式網站是單一「正式曲庫」，不公開本機 Studio、JSON 編輯器或草稿儲存功能。合成 fixture 只可透過內部 `?fixture=1` 路徑供 CI／Gate 使用，不得出現在公開導覽、正式曲庫或使用者列印內容中。

## 工作原則

1. 新需求先判斷是否影響資料格式、Gate、Workflow、樂器限制、來源政策或設計規格。
2. 有影響時先更新 `scorebook.yaml`，再修改程式與測試。
3. 旋律 event 不得內嵌歌詞；歌詞必須使用獨立 lyric track，並以 event id 對位。
4. 簡譜、五線譜與播放必須使用同一組 melody event。
5. 正式曲庫只接受 `verified` 曲目；未核對確切來源的內容不得公開。
6. 引擎測試只能使用 `fixtures/engine-fixtures.yaml` 的合成資料，不得用未驗證童謠冒充測試資料。
7. 必須明確表示小節、休止符、弱起拍及連結線；不可只靠視覺推測。
8. 英文歌曲預設使用已驗證的原文歌詞；翻譯是可選 track，未驗證翻譯不得公開。
9. 正式曲庫每首曲目提供播放、停止與單曲列印；列印只能輸出選定歌曲。
10. 正式發佈使用 branch、PR、exact head SHA CI 與必要 Gate。
11. 輸出頁面中的圖片只能作無文字裝飾，不得承載音符、歌詞、上下點或五線譜；使用者提供的樂譜圖片可作來源。
12. 不可使用 KAWAI 16 音木琴不存在的音；移調必須保留音程、節奏、小節與歌詞對位。
13. 必要 Gate 未通過時，不得標示完成、合併或發佈。
14. MusicXML、OMR、MIDI 或 AI 轉錄只屬來源證據或草稿，不得取代 `scorebook.yaml`。工具成功本身不等於內容已驗證。
15. A4 列印不得水平裁切；長曲可分頁，但只能在完整譜行之間換頁，任何譜行不得被頁界切開，也不得為硬塞一頁而縮到不可讀。

## 琴譜來源

正式內容只接受兩條來源路徑：

1. 使用者提供可檢視的樂譜網站、圖片、PDF、MusicXML 或其他靜態樂譜檔案。
2. 使用者只提供曲名，由助理上網找到可檢視的靜態樂譜或 MusicXML，固定確切版本後再轉錄。

使用者提供的來源優先決定旋律版本。來源是私人圖片或檔案時，正式資料必須記錄檔名或檔案參照與 SHA-256；不因驗證需要而把有著作權的原檔放入公開 repository。

YouTube、影片、音訊錄音及記憶不得作為旋律轉錄或正式驗證來源。找不到可核對的靜態樂譜或 MusicXML 時，停止製作並回報缺少來源，不憑印象補譜。

## 頁面順序

每個譜行固定為：

1. 無框彩色簡譜
2. 五線譜
3. 歌詞

全曲預設使用同一組簡譜列與歌詞 baseline。只有 VexFlow 為單顆 `StaveNote` 產生的 pointer rectangle 顯示安全距離不足時，才允許針對該 event 的單一標籤做解除碰撞所需的最小位移。簡譜只能上移，歌詞只能下移；未碰撞標籤必須維持全曲標準位置。

換行必須同時考慮小節邊界、五線譜符號、簡譜與歌詞寬度；禁止固定用 event 數量切行。視覺 fixture 至少要產生兩個譜行，並實際涵蓋木琴最低音與最高音。

## 來源驗證

- `scorebook.yaml` 仍是唯一正式規格；MusicXML 是可重現的來源證據。
- MusicXML source Gate 只接受單聲部、單一 part 的 MusicXML 4.0 `score-partwise`。
- 官方 XSD 必須固定到明確 release 與 Git blob SHA，不得默默追蹤最新版。
- `music21` 正規化結果必須逐顆比對 scorebook 的小節、位置、音高、時值、休止符、tie 與歌詞。
- MusicXML round-trip 必須再次通過 XSD，且正規化事件不得改變。
- Verovio 僅作獨立 SVG／MIDI 第二證據，不可反向成為正式資料來源。
- 所有來源驗證結果寫入 `reports/source/`，不得把 report 當規格來源。

## 生成檔案

- `dist/` 是建置輸出，不可手工修改。
- `reports/` 是自動驗證結果，不可當成規格來源。
- 任何生成結果與 `scorebook.yaml` 不一致時，應修正資料或產生器並重新生成。

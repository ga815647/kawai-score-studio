# Kawai Score Studio

開始工作前，先完整讀取根目錄的 `scorebook.yaml`。

`scorebook.yaml` 記錄本專案的樂器音域、顏色、曲目、節奏、歌詞、排版規格、Gate 與 Workflow。HTML、簡譜、五線譜和驗證報告都由它產生，不得直接修改生成結果來修正內容。

## 工作原則

1. 新需求先判斷是否影響資料格式、Gate、Workflow、樂器限制或設計規格。
2. 有影響時先更新 `scorebook.yaml`，再修改程式。
3. 所有音符、歌詞和五線譜必須由同一組 event 資料產生。
4. 圖片只能作為無文字裝飾，不得承載音符、歌詞、上下點或五線譜。
5. 上點與下點固定在彩色框內，位於數字正上方／正下方，並繼承數字顏色。
6. 不可使用琴上不存在的音；允許移調時必須保留音程與節奏。
7. 必要 Gate 未通過時，不得標示完成或發佈。

## 執行順序

1. 讀取 `scorebook.yaml`
2. 執行內容 Gate
3. 產生 HTML
4. 執行 HTML Gate
5. 執行列印與視覺 Gate
6. 寫入 Gate 報告
7. 全部必要 Gate 通過後才可發佈

## 生成檔案

- `dist/` 為建置輸出，不可手工修改。
- `reports/` 為自動驗證結果，不可當成規格來源。
- 任何生成結果與 `scorebook.yaml` 不一致時，應修正資料或產生器並重新生成。

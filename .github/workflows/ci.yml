name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  cleanup-closed:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "EXP-ARTIFACT-CLEANUP-KAWAI-01 closed; durable evidence retained on branch."
